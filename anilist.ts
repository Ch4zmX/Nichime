import axios from "axios";

module.exports = {
    async getUserAnime(username: string, list: string, sort: string) {
    
    },

    async getUserManga(username: string, list: string, sort: string) {
		
    }
}